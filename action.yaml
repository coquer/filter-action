name: Filter action
description: 'Filter action'
inputs:
  list:
    description: 'Matrix list path'
    required: true
  diffBranch:
    description: 'Diff branch'
    required: false
    default: ''
  lastCommit:
    description: 'Last commit'
    required: false
    default: ''
outputs:
  filtered:
    description: 'Filtered list'
runs:
  using: 'node20'
  main: 'dist/index.js'
